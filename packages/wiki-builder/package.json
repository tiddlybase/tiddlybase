{
  "name": "@firebase-auth-loader/wiki-builder",
  "description": "In-browser trusted code running outside the sandbox",
  "version": "0.0.0",
  "private": true,
  "author": "Peter Neumark",
  "license": "Apache-2.0",
  "scripts": {
    "build": "TIDDLYWIKI_PLUGIN_PATH=../../dist/plugins tiddlywiki . --output ../../dist --build wiki --verbose",
    "build:dev": "MODE=development TIDDLYWIKI_PLUGIN_PATH=../../dist/plugins tiddlywiki . --output ../../dist --build wiki --verbose"
  },
  "dependencies": {
    "@firebase-auth-loader/plugin-storage-file": "^0.0.0"
  },
  "devDependencies": {
    "tiddlywiki": "^5.2.1"
  }
}
