{
  "name": "@firebase-auth-loader/wiki-builder",
  "description": "In-browser trusted code running outside the sandbox",
  "version": "0.0.0",
  "private": true,
  "author": "Peter Neumark",
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn workspace @firebase-auth-loader/root run move_sourcemaps && TIDDLYWIKI_PLUGIN_PATH=../../dist/plugins tiddlywiki . --output ../../dist --build wiki --verbose",
    "build:dev": "yarn run build",
    "build:debug": "NODE_OPTIONS=--inspect-brk TIDDLYWIKI_PLUGIN_PATH=../../dist/plugins tiddlywiki . --output ../../dist --build wiki --verbose"
  },
  "dependencies": {
    "@firebase-auth-loader/plugin-storage-file": "^0.0.0"
  },
  "devDependencies": {
    "tiddlywiki": "^5.2.1"
  }
}
