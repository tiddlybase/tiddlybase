{
  "name": "@tiddlybase/wiki-builder",
  "description": "In-browser trusted code running outside the sandbox",
  "version": "0.0.0",
  "private": true,
  "author": "Peter Neumark",
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn workspace @tiddlybase/root run move_sourcemaps && TIDDLYWIKI_PLUGIN_PATH=../../dist/plugins tiddlywiki . --output ../../dist --build wiki --verbose",
    "build:dev": "yarn run build",
    "build:debug": "NODE_OPTIONS=--inspect-brk TIDDLYWIKI_PLUGIN_PATH=../../dist/plugins tiddlywiki . --output ../../dist --build wiki --verbose"
  },
  "dependencies": {
    "@tiddlybase/plugin-embed-media": "workspace:*",
    "@tiddlybase/plugin-mdx": "workspace:*",
    "@tiddlybase/plugin-react": "workspace:*",
    "@tiddlybase/plugin-tiddlybase-utils": "workspace:*"
  },
  "devDependencies": {
    "tiddlywiki": "^5.2.2"
  }
}
